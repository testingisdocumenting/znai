* Add: [Guide level search](flow/search) matches queries with special characters like `List.map` or `c++` verbatim, ranking exact matches first
* Add: [Guide level search](flow/search) quoted queries like `"list map"` match verbatim only
